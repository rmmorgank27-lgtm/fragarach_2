import SwiftUI

struct DiagnosticsSettingsView:View {
    @EnvironmentObject var store:ConsoleStore
    var body:some View{ScrollView{Form{Section("Authority database"){TextField("Database path",text:$store.databasePath);Button("Choose…"){if let url=PanelService.chooseDatabase(){store.databasePath=url.path;Task{await store.refresh()}}}};Section("Calendar & session policy"){Text("An operational calendar determines when a D1 bar is closed and therefore when Fragarach is permitted to fetch it. It does not grant a provider route.").foregroundStyle(.secondary);LabeledContent("Forex spot",value:"FX_D1_V1 · OTC weekday close");LabeledContent("Crypto spot",value:"CRYPTO_D1_V1 · continuous UTC");LabeledContent("US equities",value:"US_EQUITIES_D1_V1 · regular session");LabeledContent("Australian equities",value:"AUSTRALIAN_EQUITIES_D1_V1 · regular session");Text("If a lane says Operational calendar unavailable, its instrument has no reviewed calendar declaration. Open Scheduler Diagnostics to inspect the exact instrument and then establish its market/venue calendar before retrying.").font(.caption).foregroundStyle(.secondary)};Section("Development CLI"){TextField("Repository",text:$store.repositoryPath);TextField("Python executable",text:$store.pythonPath);LabeledContent("CLI identity",value:"fragarach_ii.operations_cli.v1");LabeledContent("Source commit",value:"c77aec4 + SPEC-005 working tree")};Section("Application"){LabeledContent("Product",value:"Fragarach II");LabeledContent("Authority status",value:"Candidate Authority");LabeledContent("Minimum macOS",value:"14.0");Text("Preferences store paths and presentation choices only. Credentials and authority facts are never persisted here.").foregroundStyle(.secondary)}}.padding()}}
}
